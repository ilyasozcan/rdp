name: WINDOWS 2029 RDP (Kesintisiz Tünel)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 350

    steps:
      - name: 🔽 Checkout Repository
        uses: actions/checkout@v4

      - name: 🔧 RDP Aktif Et ve Şifre Belirle
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1
          net user runneradmin Password123!

      - name: 🚀 Pinggy TCP Tünelini Başlat (Hatasız)
        shell: pwsh
        run: |
          cmd /c "start /b ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -p 443 -R 0:127.0.0.1:3389 tcp@free.pinggy.io > pinggy.log 2>&1"
          Start-Sleep -Seconds 25

      - name: 🌐 RDP Adresini Ekrana Bas
        shell: pwsh
        run: |
          # Döngü mantığı sadeleştirildi ve parantez hataları giderildi
          for ($i = 0; $i -lt 6; $i++) {
            if (Test-Path "pinggy.log") {
              $LogData = Get-Content "pinggy.log" -Raw
              if ($LogData -match "free\.pinggy\.link") {
                break
              }
            }
            Start-Sleep -Seconds 3
          }

          Write-Host "=========================================="
          Write-Host "🎉 RDP BAĞLANTI ADRESİNİZ AŞAĞIDAKİ LOGLARIN İÇİNDEDİR:"
          Write-Host "=========================================="
          if (Test-Path "pinggy.log") {
            Get-Content "pinggy.log"
          } else {
            Write-Host "❌ Log dosyası bulunamadı."
          }
          Write-Host "=========================================="
          Write-Host "⚠️ Yukarıda gördüğünüz 'xxxx.free.pinggy.link:yyyyy' adresini kopyalayın."
          Write-Host "👤 Kullanıcı Adı: runneradmin"
          Write-Host "🔑 Şifre: Password123!"
          Write-Host "=========================================="

      - name: ⏳ RDP'yi Açık Tut
        run: |
          while ($true) {
            Start-Sleep -Seconds 60
          }
