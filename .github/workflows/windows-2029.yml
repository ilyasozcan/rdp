name: WINDOWS 2029 RDP (Serveo Kesin Çözüm)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: 🔽 Checkout Repository
        uses: actions/checkout@v4

      - name: 🔧 RDP Aktif Et ve Şifre Belirle
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          # ÇÖZÜM 1: NLA (Ağ Düzeyinde Kimlik Doğrulama) kapatıldı. İç hata sorununu bu çözer.
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 0
          net user runneradmin Password123!

      - name: 🚀 Sunucuyu Güvenli SSH Köprüsüne Bağla
        shell: pwsh
        run: |
          # ÇÖZÜM 2: Rastgele ve kimsenin kullanmadığı benzersiz bir isim üretiyoruz (Örn: githrdp83726)
          $RandomID = Get-Random -Minimum 10000 -Maximum 99999
          $Alias = "githrdp" + $RandomID
          Set-Content -Path "alias.txt" -Value $Alias
          
          # Serveo'ya ismi dinamik olarak gönderiyoruz
          Start-Process -FilePath "ssh" -ArgumentList "-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ServerAliveInterval=15 -R ${Alias}:3389:127.0.0.1:3389 serveo.net" -WindowStyle Hidden
          Start-Sleep -Seconds 10

      - name: 🌐 RDP Bağlantı Komutunu Ekrana Bas
        shell: pwsh
        run: |
          $Alias = Get-Content -Path "alias.txt"
          Write-Host "=========================================="
          Write-Host "🎉 SERVED RDP BAĞLANTINIZ KESİN OLARAK HAZIR!"
          Write-Host "=========================================="
          Write-Host "Kendi bilgisayarınızda normal CMD (Komut İstemi) açın ve şu kodu yapıştırıp ENTER'a basın:"
          Write-Host "ssh -L 3333:${Alias}:3389 serveo.net"
          Write-Host "=========================================="
          Write-Host "Kodu yazdıktan sonra CMD penceresini kapatmayın (Açık kalsın)."
          Write-Host "Şimdi Uzak Masaüstü programını açıp Bilgisayar kısmına aynen şunu yazın:"
          Write-Host "127.0.0.1:3333"
          Write-Host "=========================================="
          Write-Host "👤 Kullanıcı Adı: runneradmin"
          Write-Host "🔑 Şifre: Password123!"
          Write-Host "=========================================="

      - name: ⏳ RDP'yi Açık Tut
        run: |
          while ($true) {
            Start-Sleep -Seconds 60
          }
